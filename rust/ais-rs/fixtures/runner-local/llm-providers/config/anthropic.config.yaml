schema: ais-runner/0.0.1

llm:
  provider: anthropic
  model: claude-3-5-sonnet-latest
  api_key: ${ANTHROPIC_API_KEY}
  # api_base is optional; default is https://api.anthropic.com

chains:
  eip155:1:
    rpc_url: ${EVM_RPC_URL}
