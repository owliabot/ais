# AISRS-FIX-002 plan/trace/checkpoint fixtures

Fixture set for `ais-engine` + `ais-runner` regression tests around:

- plan diff (`plan-diff/*`)
- trace replay (`trace/*`)
- checkpoint replay (`checkpoint/*`)
- trace redaction (`redaction/*`)

All files are deterministic and suitable for snapshot-style assertions.
