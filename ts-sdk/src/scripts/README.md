# `src/scripts`

Build and maintenance scripts for schema/tooling workflows.

## Files

- `build-schema.ts`: Generates JSON Schema artifacts, including workflow schema `ais-flow/0.0.3`.

## Notes

- Keep generated schema IDs and titles aligned with `specs/` and `schemas/`.
